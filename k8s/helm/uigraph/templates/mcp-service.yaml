apiVersion: v1
kind: Service
metadata:
  name: {{ include "uigraph.componentFullname" (dict "root" . "component" "mcp") }}
  labels:
    {{- include "uigraph.componentLabels" (dict "root" . "component" "mcp") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.mcp.port }}
      targetPort: http
  selector:
    {{- include "uigraph.componentSelectorLabels" (dict "root" . "component" "mcp") | nindent 4 }}
