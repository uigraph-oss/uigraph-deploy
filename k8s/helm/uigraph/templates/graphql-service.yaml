apiVersion: v1
kind: Service
metadata:
  name: {{ include "uigraph.componentFullname" (dict "root" . "component" "graphql") }}
  labels:
    {{- include "uigraph.componentLabels" (dict "root" . "component" "graphql") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.graphql.port }}
      targetPort: http
  selector:
    {{- include "uigraph.componentSelectorLabels" (dict "root" . "component" "graphql") | nindent 4 }}
