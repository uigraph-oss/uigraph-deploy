apiVersion: v1
kind: Service
metadata:
  name: {{ include "uigraph.componentFullname" (dict "root" . "component" "ui") }}
  labels:
    {{- include "uigraph.componentLabels" (dict "root" . "component" "ui") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    {{- include "uigraph.componentSelectorLabels" (dict "root" . "component" "ui") | nindent 4 }}
